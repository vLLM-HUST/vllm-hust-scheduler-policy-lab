# Legacy patch archive

This directory preserves all original commits from core PRs #267-#270 as
unmodified `git format-patch` exports, retaining author metadata and exact diffs.

Policy modules may be extracted as candidates. Runtime transitions, recapture
observation, and telemetry require explicit host contracts.
